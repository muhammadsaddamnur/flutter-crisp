name: crisp
description: Connect with Crisp Chat, register a user to chat (or not) and
  render a chat widget.
version: 0.3.1
homepage: https://github.com/lunahq/flutter-crisp

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=1.12.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^5.7.2+3
  url_launcher: ^6.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter: null
